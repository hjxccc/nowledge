# 案例 · 框架原理 → 深色架构图解（单件样板）

> ⚠️ **这是一个部分样板**，只演示六件套里的 **第②件（深色 SVG 架构图解）** 及其接地，
> 不含完整路线/练习/checkpoint。完整深学包看 `../loop-engineering-nowledge/`；
> 复训+带读外部真源看 `../deeplearning-ai-nowledge/`。

演示点：把一个新框架（Hermes agent）的机制**接地在真源上**（见 `sources/sources.md`，
来自 context7 的 `conversation_loop.py`/`agent-loop.md`），再画成过 Mayer 清单的
**深色 SVG 图解**（`diagrams/hermes-architecture.html`）——而不是凭训练记忆现编一张图。

对应场景：用户"先看懂"一个复杂机制时，图解优先于大段文字（多媒体学习原则）。
